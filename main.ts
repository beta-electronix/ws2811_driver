namespace beta-electronix_WS2811 {
    //% shim=sendDataAsm
    export function sendBuffer(buf: Buffer, pin: DigitalPin) {
    }

    //% shim=setMode
    export function setBufferMode(pin: DigitalPin, mode: number) {

    }
}

